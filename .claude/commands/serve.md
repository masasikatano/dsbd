# /serve

Serve the dashboard locally.

```bash
./run_local.sh --serve-only
```

Or with a custom port:

```bash
PORT=9000 ./run_local.sh --serve-only
```

Do not run this command yourself; provide it to the user.
