# /update

Update `docs/data/latest.json` by running the data fetch pipeline.

```bash
python -m src.update
```

This overwrites `docs/data/latest.json`. If the user asks you to run it, ask for confirmation first and do not execute it yourself; suggest the command instead.
